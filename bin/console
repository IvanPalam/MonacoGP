#!/usr/bin/env php
<?php


require_once __DIR__.'/../vendor/autoload.php';

use Symfony\Component\Console\Application;
use MonacoGP\Commands;

$app=new Application();
$app->add(new MonacoGP\Commands\ReportCommand());
$app->run();
